"""candump log analyzer. Standard library only, on purpose: it has to run on a
test rig's embedded Linux with no package manager, and it must not import the
code it is diagnosing (projects 01 / 02)."""

__version__ = "0.1.0"
